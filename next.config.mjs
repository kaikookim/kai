/** @type {import('next').NextConfig} */

const nextConfig = {
  output: "export",
  basePath: "",
  images: {
    unoptimized: true,
    path: "https://kaikookim.github.io/kai/",
  },
};

export default nextConfig;
